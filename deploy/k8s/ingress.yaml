apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ows-chem
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ows-chem
                port:
                  number: 8000
