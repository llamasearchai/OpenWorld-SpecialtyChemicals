[tox]
envlist = py310,py311,lint
isolated_build = True

[testenv]
deps = pytest
commands = pytest -q

[testenv:lint]
deps = ruff
commands = ruff check openworld_specialty_chemicals tests

 [tox]
 envlist = lint
 skipsdist = true
 isolated_build = true

 [testenv]
 basepython = python3

 [testenv:lint]
 description = Run ruff lint checks
 skip_install = true
 deps = ruff>=0.5
 commands = ruff check .
